<ivysettings>

    <settings defaultResolver="chain"/>
    <resolvers>
        <chain name="chain" returnFirst="true">
            <ibiblio name="m2local" root="file://${user.home}/.m2/repository/" m2compatible="true"/>
            <ibiblio name="ibiblio" m2compatible="true"/>
        </chain>
    </resolvers>

</ivysettings>
